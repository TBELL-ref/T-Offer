# T-Offer public UI (GitHub Pages)

**Git:** https://github.com/TBELL-ref/T-Offer.git

Static board under `docs/`. Deploy Pages from `/docs` (Settings → Pages → Deploy from branch → `/docs`).

Requires Supabase migration `034_offer_schema.sql` for `get_offer_dashboard` / `get_offer_published_snapshot`.
